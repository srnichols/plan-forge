# Phase TEMPER-04 — Visual analyzer (quorum-mode vision + diff)

Closes Phase TEMPER-04. Fifth scanner in the Tempering arc.
**Both slices executed autonomously by `pforge run-plan --quorum=power`.**

> Orchestrator: `claude-opus-4.6` via `gh copilot --yolo`.
> Quorum dispatch: claude/gpt/grok (3/3 legs, $1.05 + $1.24 reviewer).
> Run: 30m 1s total, $0.06 run cost.
> 1376/1376 tests green, 47 tools registered.

## Slice 04.1 — Pixel diff + single-model analyzer (~16m 35s)

**New modules:**
- `pforge-mcp/tempering/baselines.mjs` (230 lines) — baseline storage
  + promotion + `diffImages` via pixelmatch/pngjs
- `pforge-mcp/tempering/scanners/visual-diff.mjs` (401 lines) —
  3-band pixel diff scanner:
  | Band | Range | Action |
  |------|-------|--------|
  | Pass | < 0.1% | auto-pass |
  | Investigate | 0.1 – 2% | single-model LLM verdict |
  | Fail | > 2% | auto-fail |

**Runner wiring:** registered as 5th scanner; `scannerCount` bumped
4 → 5. Budget cascade respected.

**Config:** `TEMPERING_DEFAULT_CONFIG` extended with `visualAnalyzer`
thresholds + `visualDiffMaxMs` budget.

**New tool:** `forge_tempering_approve_baseline` (promotes current
capture to baseline).

**Dependencies:** `pixelmatch ^6.0.0`, `pngjs ^7.0.0`.

**Dashboard:** `tempering-visual-regression-detected` +
`tempering-baseline-promoted` event handlers.

**Tests:** +33 in `tempering-visual-diff.test.mjs` (558 lines).
Existing scanner-count assertions updated.

## Slice 04.2 — Quorum + dashboard viewer (~13m 24s)

**Quorum dispatch** — 2-of-3 multi-model voting for the Investigate
band. Configurable models, agreement threshold, per-leg timeout,
shared cost cap. Single-model backward compat via `mode: "single"`.

**Dashboard visual regression viewer** — baseline/current/diff image
trio; per-model vote badges; verdict banner (*Human Review Needed*
for inconclusive); action buttons (approve-as-baseline / open-bug /
ignore-once).

**L3 decision capture** — quorum verdicts recorded in OpenBrain,
**text-only, never images**.

**Server endpoints:**
- `GET /api/tempering/artifact` — path-traversal-safe artifact serve
- `POST /api/tempering/bug-stub` — TEMPER-06 placeholder

**Hub event enriched** with `verdict`, `quorum`, and `artifacts` fields.

**Tests:** +24 quorum tests + 8 dashboard/server contract tests in
`tempering-visual-quorum.test.mjs` (522 lines).

## Forbidden actions (enforced)

- Baseline images never committed (`.forge/tempering/artifacts/` gitignored)
- Artifact endpoint validates path via `isInside(<root>, <path>)` to
  block `../` traversal
- LLM receives decoded diff bytes only, never raw file paths
- L3 memory captures verdict metadata only — **no image content**
- Quorum cost-cap honored (bails out if cumulative spend ≥ cap)

## Closure tweaks (manual, post-run)

- `VERSION`: `2.44.0-dev` → `2.45.0-dev`
- `docs/plans/Phase-TEMPER-04.md`: frontmatter `status: draft` →
  `complete`; header `📝 DRAFT` → `✅ COMPLETE`; both slice headings
  gain `✅` markers

Phase TEMPER-04 closed. Arc progress: 4/6 phases complete.
